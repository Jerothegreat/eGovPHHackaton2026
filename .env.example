# Browser environment only. Keep provider credentials in Supabase Edge
# Function secrets; VITE_* values are embedded in the browser bundle.
VITE_EGOV_SSO_USE_MOCK=true
VITE_EREPORT_USE_MOCK=false
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Demo SMS recipient list. The eMessage credential is an Edge Function secret.
VITE_EMESSAGE_SMS_RECIPIENTS=
